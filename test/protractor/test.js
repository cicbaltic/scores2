describe('scores2 app', function() {
    // it('title should be "Scoring App"', function() {
    //     browser.get('https://scores2.mybluemix.net');
    //
    //     expect(browser.getTitle()).toEqual('Scoring App');
    //
    //     var appTitle = browser.getTitle().then(function(title) {
    //         // console.log('title ' + title);
    //     });
    // });
    // it('"info.NAME" in main page sould be "Hackathon name"', function() {
    //     element(by.binding('info.NAME')).getText().then(function(text) {
    //         expect(text).toEqual('Hackathon name');
    //     });
    // });
});
