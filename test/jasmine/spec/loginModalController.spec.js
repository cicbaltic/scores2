
describe ('login modal', function () {
    beforeEach(module ('app'));
    beforeEach(inject(function(_$controller_){
        $controller = _$controller_;
    }));
    // it ('animationsEnabled should be true', function () {
    //     var $scope = {};
    //     var controller = $controller('LoginModal', { $scope: $scope });
    //     expect($scope.animationsEnabled).toBe(true)
    // });
});
